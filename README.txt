fiscmd README

source code: open the .cs file

want the same project setup (.csproj, icon, build stuff, etc)???
copy all files in this folder except:
- readme
- .cs source file

then paste the code into Program.cs

thanks for checking this out :D

also important:
to compile our the app
- if ur on windows, run "build n compile.bat" to build + compile the .exe
- if ur on linux:
    > run "build n compile windows LINUX ONLY" through terminal to build + compile the .exe
    > or run "build n compile linux" through terminal to do the same thing except with linux version with no file extension

if ur on linux and sucessfully compiled out the linux one (NOT .EXE FILE), run it through the terminal
example: run "./fis" in this README's directory